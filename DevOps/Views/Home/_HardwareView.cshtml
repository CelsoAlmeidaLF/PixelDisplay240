<div id="hardware-view" class="view-panel">
    <div style="max-width: 600px; margin: 4rem auto; background: var(--card-bg); padding: 2rem; border-radius: 20px; border: 1px solid var(--border); box-shadow: var(--shadow);">
        <div style="display: flex; align-items: center; gap: 1rem; margin-bottom: 2rem;">
            <i data-lucide="cpu" class="icon" style="color: var(--primary); width: 32px; height: 32px;"></i>
            <h2 style="margin: 0;">Configuração de Hardware</h2>
        </div>

        <div class="tool-group">
            <label>Controlador & Display</label>
            <div class="grid">
                <div class="input-field">
                    <span>Controlador</span>
                    <select id="hw-controller">
                        <option value="ST7789">ST7789 (IPS)</option>
                        <option value="ILI9341">ILI9341 (TFT)</option>
                        <option value="SSD1306">SSD1306 (OLED)</option>
                        <option value="GC9A01">GC9A01 (Circular)</option>
                    </select>
                </div>
                <div class="input-field">
                    <span>Resolução</span>
                    <select id="hw-resolution">
                        <option value="240x240">240x240 px</option>
                        <option value="240x320">240x320 px</option>
                        <option value="128x64">128x64 px</option>
                        <option value="320x480">320x480 px</option>
                    </select>
                </div>
            </div>
        </div>

        <div class="tool-group">
            <label>Pinagem SPI (Interface)</label>
            <div class="grid" style="grid-template-columns: repeat(3, 1fr);">
                <div class="input-field"><span>MOSI (SDA)</span><input type="number" id="pin-mosi" value="23"></div>
                <div class="input-field"><span>SCK (SCL)</span><input type="number" id="pin-sck" value="18"></div>
                <div class="input-field"><span>MISO</span><input type="number" id="pin-miso" value="19"></div>
            </div>
        </div>

        <div class="tool-group">
            <label>Sinais de Controle</label>
            <div class="grid" style="grid-template-columns: repeat(2, 1fr);">
                <div class="input-field"><span>CS (Chip Select)</span><input type="number" id="pin-cs" value="5"></div>
                <div class="input-field"><span>DC (Data/Cmd)</span><input type="number" id="pin-dc" value="2"></div>
                <div class="input-field"><span>RST (Reset)</span><input type="number" id="pin-rst" value="4"></div>
                <div class="input-field"><span>BL (Backlight)</span><input type="number" id="pin-bl" value="15"></div>
            </div>
        </div>

        <div class="tool-group">
            <label>Velocidade SPI</label>
            <select id="hw-spi-speed">
                <option value="80000000">80.0 MHz (Ultra Fast)</option>
                <option value="40000000">40.0 MHz (Fast)</option>
                <option value="20000000">20.0 MHz (Standard)</option>
                <option value="10000000">10.0 MHz (Slow/Safe)</option>
            </select>
        </div>

        <button class="primary-btn" style="width:100%; margin-top:1rem; height: 50px; font-size: 1rem;"><i data-lucide="save"></i> Salvar Configuração de Hardware</button>
        <p style="font-size: 0.7rem; color: var(--text-muted); margin-top: 1rem; text-align: center;">Esses pinos serão incluídos automaticamente como definições no código exportado.</p>
    </div>
</div>
